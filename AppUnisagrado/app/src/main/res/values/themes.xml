<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AppUnisagrado" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/vermelhoUnisagrado</item>
        <item name="colorPrimaryVariant">@color/vermelhoUnisagrado</item>
        <item name="colorOnPrimary">@color/vermelhoUnisagrado</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/black</item>
        <item name="colorSecondaryVariant">@color/black</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.AppUnisagrado.NoActionBar">
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
        @link android.content.pm.ActivityInfo#SCREEN_ORIENTATION_PORTRAIT
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:screenOrientation">portrait</item>
        <item name="statusBarForeground">@color/vermelhoUnisagrado</item>
    </style>

    <style name="Theme.AppUnisagrado.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.AppUnisagrado.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>