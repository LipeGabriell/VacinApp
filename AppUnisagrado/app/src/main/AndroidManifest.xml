<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appunisagrado" >

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/unisagra_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/unisagra_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppUnisagrado" >

        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAVPYGDe2fuLKILfQcV9kas7-q-r-BuvFI" />

        <activity
            android:name=".mapa"
            android:exported="false"
            android:label="@string/title_activity_mapa" />
        <activity
            android:name=".dados"
            android:exported="false"
            android:label="@string/title_activity_dados"
            android:theme="@style/Theme.AppUnisagrado.NoActionBar" />
        <activity
            android:name=".recuperacao"
            android:exported="false"
            android:label="@string/title_activity_recuperacao"
            android:theme="@style/Theme.AppUnisagrado.NoActionBar" />
        <activity
            android:name=".sintomas"
            android:exported="false"
            android:theme="@style/Theme.AppUnisagrado.NoActionBar" />
        <activity
            android:name=".infos"
            android:exported="false"
            android:theme="@style/Theme.AppUnisagrado.NoActionBar" />
        <activity
            android:name=".Principal"
            android:exported="false"
            android:theme="@style/Theme.AppUnisagrado.NoActionBar" />
        <activity
            android:name=".prevencao"
            android:exported="false"
            android:theme="@style/Theme.AppUnisagrado.NoActionBar" />
        <activity
            android:name=".loading"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>